apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .ModsCMName }}
  namespace: minecraft-modded
  labels:
    app: {{ .DeploymentName }}
    instance: "{{ .Number }}"
data:
  mods.txt: |
{{ .IndentModsTxt }}
